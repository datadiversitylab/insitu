#' Identifies nodes where multiple sequential in-situ speciation events occur within a defined time window. Useful for adaptive radiations
#'
#' @export

detect_radiation_bursts <- function(){
  return(1+1)
}
